import objectAssign from 'object-assign';

const initialState = {
  isLoggedIn: false,
  user: false
}

export function auth(state = initialState, action) {
  switch(action.type) {
    default:
    return initialState;
  }
}
